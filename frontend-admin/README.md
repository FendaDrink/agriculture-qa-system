# frontend-admin

基于 React + TypeScript + Ant Design + Vite 的管理端。

## 本地开发

1. 安装依赖

```bash
npm install
```

2. 启动开发服务器

```bash
npm run dev
```

默认端口为 `8000`，请求会通过 `/api` 代理到 `http://localhost:3000`。
